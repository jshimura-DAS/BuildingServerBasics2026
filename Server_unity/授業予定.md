| 週 | 授業内容 | 到達目標・学習課題など |
|---|---|---|
| 16 | Python環境構築と基本：venvを用いた仮想環境の役割を学び、開発環境を一人で構築できる。基本的な変数の宣言や制御構文を学習し、ゲーム開発で馴染みのあるC#との文法的な共通点や相違点を整理して解説できる。 | 目標：仮想環境を構築し基本構文をC#と比較説明できる。課題：venvの利点をノートに整理。 |
| 17 | Python文法：辞書とリスト：JSONデータとの親和性が高い「辞書型」と「リスト型」の操作手法を習得する。データの追加、更新、抽出、ネスト構造の扱いを実習し、API開発で必要となるデータ構造の構築能力を養う。 | 目標：辞書型データを自在に操作し抽出できる。課題：多重構造の辞書データを作成する。 |
| 18 | Flask入門：Pythonの軽量フレームワークFlaskの基本構造を学び、最小構成のWebサーバーを起動する。ルーティングの設定を行い、ブラウザからのリクエストに対して「Hello World」を表示させるまでのプロセスを実践する。 | 目標：Flaskサーバーを起動し画面表示を実演できる。課題：独自のルーティングを追加。 |
| 19 | Web APIの設計（JSON）：辞書型データをJSON形式に変換し、HTTPリクエストに対して適切なヘッダーと共にレスポンスを返すWeb APIを作成する。クライアント（Unity）が解釈しやすいデータ構造の設計手法を習得する。 | 目標：辞書データをJSON形式でAPI出力できる。課題：APIレスポンスの階層図を描く。 |
| 20 | Python ⇔ SQLite連携：SQLite3ライブラリを用い、Pythonからデータベースへ接続する。SQL文を発行してテーブル作成、データの挿入、取得をプログラムから実行し、永続的なデータ管理を行うバックエンド機能を実装する。 | 目標：PythonからSQLを実行しDB操作を完遂できる。課題：基本SQL文のスクリプトを書く。 |
| 21 | スコア登録APIの作成：Unity等の外部クライアントから送信されるPOSTリクエストを受け取り、JSON形式のボディをパースする。受け取ったプレイヤー名やスコアを、事前に構築したSQLiteのテーブルに保存するロジックを実装する。 | 目標：POSTされたデータをDBに保存するAPIを実装できる。課題：データの受渡図を作成。 |
| 22 | Unity側：通信基礎：UnityWebRequestクラスを使用し、自作APIに対して非同期リクエストを送信するプログラムを作成する。GET/POSTの使い分けを理解し、サーバーとの通信開始から終了までのフローをC#で実装する。 | 目標：UnityからAPIへリクエストを送信・実行できる。課題：UnityWebRequestの引数を調査。 |
| 23 | Unity側：JSONパース：サーバーから返されるJSON文字列を受け取り、C#のクラスオブジェクトに変換（デシリアライズ）する手法を学ぶ。JsonUtility等のライブラリを活用し、通信結果をゲーム内で利用可能なデータとして扱う。 | 目標：受信したJSONをC#クラスに正常に変換できる。課題：JSONに対応するクラス定義を書く。 |
| 24 | 実践：ランキング機能：SQLの「ORDER BY」句を使い、スコアの高い順にデータを抽出するAPIを作成する。Unity側で受信した複数件のデータをUI（TextやList）に反映させ、オンラインランキングの基本表示機能を実装する。 | 目標：抽出したスコア一覧をUnity上で表示・実演できる。課題：ランキング取得のSQLをメモ。 |
| 25 | 発展：サーバーサイドロジック：スコアの妥当性検証（バリデーション）手法を学ぶ。想定外の大きな値や負の値などの不正入力をサーバー側で検知・拒否するロジックを組み込み、ゲームの公平性を保つためのバックエンド処理を実装する。 | 目標：不正なスコア登録を検知し遮断するロジックを組める。課題：異常値の判定基準を整理。 |
| 26 | セキュリティと通信の保護：APIキーによる簡易認証機能を実装し、特定のクライアント以外からのアクセスを遮断する手法を習得する。通信の安全性を高めるための基礎知識を学び、公開後のシステムを第三者の悪用から守る防護策を施する。 | 目標：APIキー認証を実装し部外者のアクセスを防げる。課題：APIキーの秘匿方法を検討。 |
| 27 | 総合演習：ゲーム連携(1)：これまで学んだ技術を統合し、自身のUnityゲームプロジェクトにサーバー連携機能を組み込む。ランキングの表示やハイスコアの保存場所をローカルから自作サーバーへ移行し、オンライン対応仕様に改造する。 | 目標：自分のプロジェクトにAPI通信機能を実装できる。課題：改造箇所のリストを1枚作成。 |
| 28 | 総合演習：ゲーム連携(2)：オフライン時やサーバーエラー時の例外処理を実装する。タイムアウトの検知や再試行ロジックを組み込み、通信環境が不安定な状況下でもゲームが停止（クラッシュ）しない堅牢なシステムへと仕上げる。 | 目標：通信エラー時の処理を適切に実装し動作を確認できる。課題：エラー時の挙動を整理。 |
| 29 | デバッグとデプロイ：ローカルホスト以外からの接続を確認する。ホストPCのIPアドレスを特定し、LAN内の他のデバイスからアクセス可能な状態にデプロイを行う。リリース直前を想定した最終的な動作テストを完遂する。 | 目標：外部デバイスから通信可能な状態へデプロイできる。課題：自分のIPを他者へ通知。 |
| 30 | 最終成果発表：構築したサーバーと連携したUnityゲームを実演する。オンラインランキングへの登録・取得が正常に動作すること、エラーハンドリングやセキュリティ設定が機能していることをデモンストレーション形式で発表・評価する。 | 目標：サーバー連携したゲームシステムを外部へ公開・発表できる。課題：他者の実装の良い点を3つメモ。 |